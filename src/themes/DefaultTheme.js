import { createMuiTheme } from "@material-ui/core/";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#43425D"
    },
    secondary: {
      main: "#A5A4BF"
    }
  },
  typography: {
    fontSize: 12.5
  }
});

export default theme;
